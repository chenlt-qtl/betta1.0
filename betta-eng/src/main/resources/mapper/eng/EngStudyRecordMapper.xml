<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.betta.eng.mapper.EngStudyRecordMapper">
    <resultMap id="RecordMap" type="EngStudyRecord">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="articleId" column="article_id"/>
        <result property="articleTitle" column="article_title"/>
        <result property="score" column="score"/>
        <result property="correctCount" column="correct_count"/>
        <result property="totalCount" column="total_count"/>
        <result property="passed" column="passed"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
    </resultMap>
    <insert id="insertEngStudyRecord" parameterType="EngStudyRecord" useGeneratedKeys="true" keyProperty="id">
        insert into eng_study_record(user_id,article_id,score,correct_count,total_count,passed,create_by,create_time)
        values(#{userId},#{articleId},#{score},#{correctCount},#{totalCount},#{passed},#{createBy},now())
    </insert>
    <select id="selectRecentByUserId" resultMap="RecordMap">
        select r.*,a.title article_title
        from eng_study_record r join eng_article a on a.id=r.article_id
        where r.user_id=#{userId}
        order by r.create_time desc,r.id desc limit 10
    </select>
    <select id="countByUserId" resultType="long">
        select count(*) from eng_study_record where user_id=#{userId}
    </select>
    <select id="sumScoreByUserId" resultType="long">
        select coalesce(sum(score),0) from eng_study_record where user_id=#{userId}
    </select>
    <select id="selectLatestByUserAndArticle" parameterType="EngStudyRecord" resultMap="RecordMap">
        select * from eng_study_record
        where user_id=#{userId} and article_id=#{articleId}
        order by create_time desc,id desc limit 1
    </select>
</mapper>
